spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=empresa_system
    username: postgres
    password: empresa-system-db
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: postgres-pool
      maximum-pool-size: 10